# Plugin Testing Environment with Docker & PHPUnit

A complete environment for wordpress plugins development.

### Run on the compose bash the following command

`wp scaffold plugin my-plugin --allow-root`